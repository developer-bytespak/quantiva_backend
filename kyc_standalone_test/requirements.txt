# KYC Standalone Testing Requirements

# Face Recognition - Primary (if build tools available)
# insightface>=0.7.3

# Face Recognition - Fallback (works on all platforms)
deepface>=0.0.79

# ONNX Runtime (for model inference)
onnxruntime>=1.16.0

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# OCR (optional - for document text extraction)
easyocr>=1.7.0

# Utilities
scikit-learn>=1.3.0
