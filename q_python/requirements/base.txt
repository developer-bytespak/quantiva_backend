fastapi>=0.95.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
pandas-ta>=0.3.14b0
opencv-python>=4.8.0
requests>=2.31.0
fredapi>=0.5.1

# News API Endpoint Dependencies (LunarCrush + FinBERT Sentiment Analysis)
# - requests: For LunarCrush API calls (already listed above)
# - python-dateutil: For date parsing in news services (LunarCrush, Stock News)
# - python-dotenv: For loading LUNARCRUSH_API_KEY from .env file (already listed below)
python-dateutil>=2.8.2

# OCR libraries (disabled for memory optimization)
# easyocr>=1.7.0  # Removed - too heavy for 512MB limit
# pytesseract>=0.3.10  # Requires Tesseract OCR to be installed on system

# Face recognition (DeepFace only - InsightFace removed)
deepface>=0.0.79
# ONNX Runtime (for model inference)
onnxruntime>=1.16.0
# Utilities for face matching
scikit-learn>=1.3.0

# ML Model Dependencies (FinBERT)
# FinBERT: Financial sentiment analysis using ProsusAI/finbert model
# - Model (ProsusAI/finbert) is automatically downloaded from Hugging Face on first use (438MB)
# - Download occurs when dashboard is first opened and FinBERT is initialized
# - Model is cached in ~/.cache/huggingface/ after first download
# - Requires transformers, torch, and huggingface-hub for model loading
transformers>=4.32.0
torch>=2.0.0
huggingface-hub>=0.16.0

# Environment and Database
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0

# LLM Dependencies
google-generativeai>=0.3.0  # Gemini (fallback)
openai>=1.0.0                # OpenAI (primary)

tf-keras>=2.20.0